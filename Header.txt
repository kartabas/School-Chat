								School Chat

This is a school chat where you can add your class and chat with other students at the school. 



Commands:
//Comits history
git hist

// Reset code on github repository
git push -f origin  9756c39:main

// Reset code on  localhost
git reset --hard 9756c39


//Merge branch with main

git checkout master
git pull origin master
git merge test
git push origin master

//Create .jar file
mvn clean package -DskipTests
mvn install -DskipTests

//Delete all completed container 
docker container prune

//Update image
docker build -t springboot-app .



//Start and build app
docker-compose up --build

docker-compose up -d


//Stop docker compose
docker-compose down





//Database connect
localhost
postgres
admin
5432


//connect to database from container 
docker exec -it c9cec7daadac psql -U postgres

\l

\c postgres

\dt

SELECT * FROM your_table_name;


//-------------------------------------

//custom shadow box
// https://www.cssmatic.com/box-shadow

//for start react 
//Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass (Powershell)
//npm start

//---------------------------------------------Nginx-------------------------------------------------
//show logs on the server 
sudo nano /etc/nginx/nginx.conf

log_format device_log '$remote_addr "$http_user_agent"';

access_log /var/log/nginx/device.log device_log;

sudo systemctl reload nginx

sudo tail -f /var/log/nginx/device.log

//For example
// a.b.c.d "Mozilla/5.0 (iPhone; CPU iPhone OS 17_2 like Mac OS X) AppleWebKit/605.1.15 (KHTML, like Gecko) Version/17.2 Mobile/15E148 Safari/604.1"

//---------------------------------------------Nginx-------------------------------------------------









